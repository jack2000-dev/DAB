[project]
name = "dab"
version = "0.1.0"
description = "Data Analysis Bible — a searchable wiki for data analysis"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "zensical>=0.0.39",
]
